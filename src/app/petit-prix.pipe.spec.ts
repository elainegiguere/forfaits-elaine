import { PetitPrixPipe } from './petit-prix.pipe';

describe('PetitPrixPipe', () => {
  it('create an instance', () => {
    const pipe = new PetitPrixPipe();
    expect(pipe).toBeTruthy();
  });
});
