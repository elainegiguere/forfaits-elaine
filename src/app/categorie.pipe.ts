import { Pipe, PipeTransform } from '@angular/core';
import { Forfait } from './forfait';

// @Pipe({
//   name: 'categorie'
// })
// export class CategoriePipe implements PipeTransform {

//   transform(forfaits: Forfait[],categorie:string): Forfait[] {
  
//     return forfaits.filter(forfait => forfait.categories.includes(categorie));
//   }

// }
